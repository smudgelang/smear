language: c
compiler: gcc
os: linux
dist: trusty
sudo: required
addons:
  apt:
    packages:
      devscripts
      fakeroot
      #valgrind
install:
- sudo add-apt-repository -y ppa:jonathonf/debhelper
- sudo apt-get -qq update
- sudo apt-get install -y debhelper
script: make all runtests
