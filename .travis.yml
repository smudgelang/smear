language: c
compiler: gcc
os: linux
dist: trusty
sudo: required
addons:
  apt:
    packages:
      devscripts
      fakeroot
      #valgrind
install:
- sudo add-apt-repository -y "deb http://ppa.launchpad.net/jonathonf/debhelper/ubuntu xenial main"
- sudo apt-get -qq update
- sudo apt-get install debhelper dh-strip-nondeterminism
script: make all runtests
